import { Box } from '@mui/material';
import { BaseLayer } from './base-layer/BaseLayer';

export const Canvas = () => {
  return (
    <Box sx={{ margin: 'auto' }}>
      <BaseLayer />
    </Box>
  );
};
