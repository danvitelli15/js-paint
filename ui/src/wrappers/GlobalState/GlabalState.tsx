import React from "react";

import { WrapperPropTypes } from "../../utilities/interfaces/wrapperPropTypes";

export const GlobalState = ({ children }: WrapperPropTypes) => {
  return <>{children}</>;
};
